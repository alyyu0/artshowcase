# Copy this to .env and fill values

# Supabase DB connection (Full connection string from Supabase -> Settings -> Database -> Connection string)
DATABASE_URL=postgresql://<db_user>:<db_pass>@db.ulpspecqldijdxjzhlyv.supabase.co:5432/<db_name>?sslmode=require

# Supabase project info (used by backend supabase client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# JWT secret for auth
JWT_SECRET=your_jwt_secret

# Frontend origin for CORS (optional)
FRONTEND_ORIGIN=http://localhost:5173
